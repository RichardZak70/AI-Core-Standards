# Template for recording default model settings and provider catalog.

default:
  provider: openai
  model: gpt-4.1
  temperature: 0.1
  max_tokens: 1024

providers:
  openai:
    default_model: gpt-4.1
  anthropic:
    default_model: claude-3-5-sonnet
