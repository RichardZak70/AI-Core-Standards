# Vale configuration (prose linting) for docs/ and README.md.
# Keep this deterministic by vendoring our minimal style rules in .github/styles.

StylesPath = .github/styles
MinAlertLevel = warning

[*.md]
BasedOnStyles = AI-Core-Standards
